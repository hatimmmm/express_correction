<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All My Books</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="container p-5">
      <h1 class="text-6xl mb-20">Books</h1>
      <h4 class="text-4xl mb-4">List of books :</h4>
      <ul>
        <% books.forEach( (book )=> { %>
        <li class="outline rounded mb-4 p-5 flex justify-between">
          <a href="books/<%= book.id %>" class="text-3xl"><%= book.title %></a>
          <form action="books/<%= book.id %>" method="POST">
            <input type="hidden" name="_method" value="DELETE" />
            <input type="submit" value="X" class="bg-red-500 rounded p-3" />
          </form>
        </li>
        <% }) %>
      </ul>
      <%- include('new') %>
    </div>
  </body>
</html>
